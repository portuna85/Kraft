apiVersion: 1

providers:
  - name: kraft-dashboards
    orgId: 1
    folder: KRAFT
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/dashboards
